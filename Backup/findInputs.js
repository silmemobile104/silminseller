const fs = require('fs'); const files = fs.readdirSync('.').filter(f => f.endsWith('.html')); for (const f of files) { const content = fs.readFileSync(f, 'utf8'); const matches = [...content.matchAll(/<(?:input|textarea|select)[^>]*class\s*=\s*['\x22][^'\x22]*text-(?:white|slate-[123]00)[^'\x22]*['\x22][^>]*>/gi)]; if (matches.length > 0) { console.log('File: ' + f); matches.forEach(m => console.log(m[0])); } }
